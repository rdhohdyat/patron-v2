import ShopLayout from "@/Layouts/ShopLayout";

export const CheckOut = ({ auth, order }) => {
    return (
        <ShopLayout user={auth.user}>
            <div>
                <div></div>
            </div>
        </ShopLayout>
    );
};
